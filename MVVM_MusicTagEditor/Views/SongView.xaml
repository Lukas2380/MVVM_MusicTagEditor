<UserControl
    x:Class="MVVM_MusicTagEditor.Views.SongView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
    xmlns:local="clr-namespace:MVVM_MusicTagEditor.Views"
    mc:Ignorable="d" 
    d:DesignHeight="450" d:DesignWidth="800" x:Name="stackP">
    <StackPanel>

        <ListView ItemsSource="{Binding Songs}" SelectedItem="{Binding SelectedSong}" ScrollViewer.CanContentScroll="True"
                  MaxHeight="{Binding ElementName=stackP, Path=ActualHeight}" KeyboardNavigation.DirectionalNavigation="Contained">
            <ListView.View>
                <GridView>
                    <GridViewColumn DisplayMemberBinding="{Binding SongDisplay}" Header="Title - Artists"/>
                </GridView>
            </ListView.View>
        </ListView>

    </StackPanel>
</UserControl>
