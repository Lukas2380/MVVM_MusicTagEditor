<UserControl x:Class="MVVM_MusicTagEditor.Views.InfoView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MVVM_MusicTagEditor.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <DockPanel>
        <StackPanel Margin="20" DockPanel.Dock="Top" Orientation="Horizontal">
            <TextBox Width="100" Margin="3 5 3 5" Text="{Binding Path=Title, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
            <TextBox Width="100" Margin="0 5 3 5" Text="{Binding Path=AlbumName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
        </StackPanel>
        <Button Margin="20" DockPanel.Dock="Bottom" Content="Save" Command="{Binding Path=SendDataCommand}" VerticalAlignment="Bottom"/>
    </DockPanel>
</UserControl>
